<docbook><section><title>UdadocProductsjdbc-postgres-mt</title><para> </para><title> doc.Products.jdbc-postgres-mt</title> doc.Products.jdbc-postgres-mt
<para> The Multi-Tier JDBC driver for <ulink url="PostgreSQL">PostgreSQL</ulink>  consists of a set of generic JDBC client classes and the <ulink url="PostgreSQL">PostgreSQL</ulink>  Database agent on the server the built in database independent network layer for communication between the client and server.</para><para>The <ulink url="OpenLink">OpenLink</ulink>  Multi-Tier Drivers for <ulink url="PostgreSQL">PostgreSQL</ulink>  are a Type B class driver developed using the <ulink url="PostgreSQL">PostgreSQL</ulink>  network (wire) protocol interface.</para><para>These drivers are currently available for <ulink url="PostgreSQL">PostgreSQL</ulink>  versions 6.x, 7.x and beyond.</para><para>  [[/images/aodbcst.gif|]]</para><para> /<ulink url="PostgreSQL">PostgreSQL</ulink> Multi-Tier JDBC Driver Architecture Diagram Image scaled down; Click to enlarge./</para><para> This driver is available for Windows, Linux, Unix(most flavours), Mac Classic, Mac OS X or any Java enabled Operating systems.</para><para> </para></section></docbook>