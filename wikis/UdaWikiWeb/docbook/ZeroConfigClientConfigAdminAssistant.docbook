<docbook><section><title>ZeroConfigClientConfigAdminAssistant</title><title> Configuring a Multi-Tier Client to use a ZeroConfig DSN through the HTTP-based Admin Assistant </title> Configuring a Multi-Tier Client to use a ZeroConfig DSN through the HTTP-based Admin Assistant 
<para>When configuring a DSN in the HTTP-based Admin Assistant, the Server field will offer a drop-down menu of available Zero Config targets --</para><para>During the configuration of a DSN in the Admin Assistant it is currently not possible to dynamically discover and present the Zero Config entries.
 However, you can manually put a Zero Config entry in the Server field.<figure><graphic fileref="ZeroConfigClientConfigAdminAssistant/ZCClientConfigAA.png" /></figure></para>
</section></docbook>